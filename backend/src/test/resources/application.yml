spring:
  application:
    name: real-estate-crm

  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        # Lets a test count the statements a request actually issues, which is
        # the only way to keep an N+1 from creeping back in unnoticed.
        generate_statistics: true

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: test
    password: test
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  # Base64, because JwtService decodes it as such — a plain sentence here made every
  # token-issuing test fail with "Illegal base64 character".
  secret: ZXN0YXRlLWNybS10ZXN0LXNpZ25pbmcta2V5LW5vdC11c2VkLWluLXByb2R1Y3Rpb24tMDEyMzQ1Njc4OWFiY2RlZg==
  access-token-expiration: 86400000
  refresh-token-expiration: 604800000

app:
  base-url: http://localhost:8080
  mail:
    enabled: false
    # Tests must not depend on DNS being reachable, or on a domain still
    # existing years from now.
    verify-domain: false